# 要件定義: WebVR視野狭窄体験アプリ

## 作成日時
2026年2月3日

## プロジェクト概要
A-Frameを使用したWebVRアプリケーションで、360度画像を表示しながら視線付近を除いて画面を暗転させる「視野狭窄（トンネルビジョン）」体験を提供する。

## 目的
- Pico4 Enterpriseで動作するWebVRアプリケーションの作成
- 360度画像による没入体験の提供
- 視野狭窄症状のシミュレーション体験

## ターゲットデバイス
- **VRヘッドセット**: Pico4 Enterprise
- **ブラウザ**: WebXR対応ブラウザ

## 機能要件

### 1. 360度画像表示
- **使用画像**: R0010034.JPG
- **表示方法**: Equirectangular形式の360度パノラマ画像
- **配置**: ユーザーを中心とした球体（sky）にマッピング

### 2. 視線トラッキング
- **トラッキング対象**: カメラ（ユーザーの視点）の向き
- **更新頻度**: リアルタイム（フレームごと）

### 3. 視野狭窄エフェクト
- **効果**: 視線中心部分のみ明瞭に表示し、周辺部を暗転
- **実装方法**: 
  - ビネット（vignette）エフェクトの逆転
  - 視線方向を中心とした円形または楕円形の明瞭領域
  - 周辺部は黒く（または暗く）表示
- **時間ベースの段階的変化**:
  - VRモード起動時からタイマー開始
  - 50秒サイクルで5つのステージを自動遷移
  - 各ステージ間はスムーズに線形補間（急激な変化なし）
  - 50秒後に0秒の状態にリセットしてループ
- **調整可能パラメータ**:
  - 明瞭領域の半径/サイズ（時間経過で自動変化）
  - 境界のぼかし具合（グラデーション）
  - 暗転の濃さ

### 4. VR対応
- **WebXRサポート**: VRモードでの表示
- **6DoF対応**: 頭の動きに応じた視点変更
- **Pico4最適化**: デバイス固有の設定や調整
- **自動VRモード切り替え**: 
  - ページロード時に自動的にVRモードに切り替わる
  - Pico4 EnterpriseのPicoブラウザで自動VRモードが動作
  - デスクトップ環境では、オーバーレイ表示をクリックして全画面表示に切り替え
  - 重要: `<a-scene vr-mode-ui="enabled: true">`がPicoブラウザでの自動VRモード起動の鍵

## 非機能要件

### 1. パフォーマンス
- **フレームレート**: 最低60fps、理想的には72fps以上（Pico4の推奨値）
- **レイテンシ**: 視線トラッキングから表示更新まで最小限の遅延

### 2. ユーザビリティ
- **起動の簡単さ**: URLアクセスで自動的にVRモードに入る
  - VRデバイス（Pico4など）: オーバーレイを自動で非表示にして、即座にVRモードに切り替え
  - デスクトップ環境: オーバーレイ表示（クリックでフルスクリーン表示）
- **操作性**: コントローラーでパラメータ調整可能（オプション）

### 3. 互換性
- **主要対応**: Pico4 Enterprise
- **副次的対応**: 他のWebXR対応VRデバイス（Meta Quest等）

## 技術スタック

### フレームワーク・ライブラリ
- **A-Frame**: 0.9.0以降（WebVRベースフレームワーク）
- **Three.js**: A-Frameの基盤
- **WebXR API**: VR機能の実装

### 実装方法
1. **A-Frameシーン**: 基本的なVR環境の構築
2. **カスタムシェーダー**: 視野狭窄エフェクトの実装
3. **カスタムコンポーネント**: 視線トラッキングとエフェクト制御

## 成功基準

### 必須要件
- [ ] 360度画像（R0010034.JPG）がVR空間に正しく表示される
- [ ] Pico4 EnterpriseでVRモードが起動する
- [ ] ページロード時に自動的にVRモードに切り替わる（Picoブラウザ）
- [ ] デスクトップ環境ではオーバーレイ表示され、クリックで全画面表示される
- [ ] VRデバイスではオーバーレイが自動で非表示になる
- [ ] VRモード起動時にタイマーが開始される
- [ ] 時間経過に応じて視野狭窄が段階的に変化する（5ステージ）
- [ ] 各ステージ間がスムーズに遷移する（線形補間）
- [ ] 50秒後に0秒の状態にリセットしてループする
- [ ] 視線方向の中心部分のみ明瞭に表示される
- [ ] 視線を動かすと明瞭領域も追従する
- [ ] 周辺部が適切に暗転表示される

### 望ましい要件
- [ ] エフェクトのパラメータを調整できる
- [ ] 60fps以上で安定動作する
- [ ] スムーズな視線追従（ジッターなし）

## リスクと制約

### リスク
1. **パフォーマンス問題**: シェーダー処理が重く、フレームレートが低下する可能性
2. **デバイス互換性**: Pico4特有の制約や不具合
3. **視線遅延**: トラッキング遅延により酔いを誘発する可能性

### 制約
1. **既存プロジェクト**: Laravelベースのプロジェクトに組み込む必要がある
2. **画像ファイル**: R0010034.JPGが既に存在している前提
3. **開発環境**: Windows環境での開発

## 既存資産の確認事項

### 確認済み項目
- [x] **R0010034.JPGファイルの存在と配置場所**: `public/cg/R0010034.JPG` に存在
- [x] **エフェクトの強度**: 固定値で実装
- [x] **起動パス**: `/vr-tunnel` で専用ページを作成
- [ ] 既存のA-Frame実装の有無（設計時に確認）
- [ ] 既存のVR関連コードの有無（設計時に確認）

## 確定仕様

### ファイル配置
- **360度画像パス**: `/cg/R0010034.JPG` (public/cg/R0010034.JPG)
- **アクセスURL**: `/vr-tunnel`
- **ビューファイル**: `resources/views/vr-tunnel.blade.php` (予定)

### 視野狭窄パラメータ（時間ベース動的変化）

#### タイムライン（40秒サイクル、ループ）
| 時間 | 視野角 | innerRadius | 説明 |
|------|--------|-------------|------|
| 0～5秒 | 視野狭窄なし | 1.0 | 完全に透明（エフェクトなし） |
| 5～10秒 | 30-40度程度 | 1.0～0.175 | 軽度の視野狭窄（スムーズに遷移） |
| 10～15秒 | 20-30度程度 | 0.175～0.125 | 中度の視野狭窄 |
| 15～20秒 | 10-20度程度 | 0.125～0.075 | 重度の視野狭窄 |
| 20～25秒 | 0-10度程度 | 0.075～0.025 | 極重度の視野狭窄 |
| 25～35秒 | 0.2度程度 | 0.025～0.002 | 超極重度の視野狭窄（ほぼ見えない） |
| 35～40秒 | 0.05度程度 | 0.002～0.0005 | 最も狭い視野（完全にほぼ見えない） |

#### パラメータ詳細
- **innerRadius変化**: 1.0 → 0.175 → 0.125 → 0.075 → 0.025 → 0.002 → 0.0005
- **遷移方法**: 線形補間（lerp）で各区間かけてスムーズに変化
- **outerRadius**: 
  - 0-20秒: innerRadius + 0.20（固定幅、境界のぼかし約5-10度相当）
  - 20-40秒: innerRadius + (0.20から0.05へ段階的に減少、さらに見えにくくする）
- **境界のぼかし**: 自然なグラデーション（5-10度の遷移領域）
- **暗転の濃さ**: 完全な黒（opacity: 1.0）
- **ループ**: 40秒到達後、0秒の状態（innerRadius=1.0）にリセット

#### タイマー仕様
- **開始トリガー**: VRモード起動時（`enter-vr`イベント）
- **更新頻度**: 毎フレーム（tick）で時刻をチェックし、innerRadiusを更新
- **リセット**: 40秒経過時に経過時間を0にリセット

### ルーティング
- **URL**: `/vr-tunnel`
- **メソッド**: GET
- **レスポンス**: Bladeビューまたは静的HTML

## 次のステップ
1. 設計フェーズへの移行
2. 実装アーキテクチャの決定
3. A-Frameコンポーネントの設計

---

# 要件定義: VRシューティングゲーム - ゲーム前の弾丸切り替え・発射機能

## 作成日時
2026年2月13日

## プロジェクト概要
VRシューティングゲーム（shooting3Danimal.blade.php）において、ゲームスタート前でもボールの切り替えと発射ができるようにする機能拡張。

## 目的
- ゲーム開始前のチュートリアル・練習機会の提供
- リンゴとキャベツの切り替え操作の習熟支援
- 発射感覚の事前体験

## ターゲットデバイス
- **VRヘッドセット**: Pico4 Enterprise
- **ブラウザ**: WebXR対応ブラウザ

## 機能要件

### 1. ゲーム開始前のボール切り替え機能
- **対象ボタン**: グリップ、Aボタン、Bボタン（トリガー以外）
- **動作タイミング**: ゲームスタート前（`window.gameStarted === false`）から可能
- **切り替え対象**: リンゴ（apple: poke_ball_07apple.glb）とキャベツ（cabbage: poke_ball_09cabbage.glb）
- **切り替え挙動**: 
  - ボタンを押すたびに、リンゴ ⇔ キャベツが交互に切り替わる
  - 両方のコントローラーのプレビュー表示も更新される
  - 現在選択されているボールが、コントローラー先端に小さく表示される（既存機能）

### 2. ゲーム開始前のボール発射機能
- **対象ボタン**: トリガー
- **動作タイミング**: ゲームスタート前（`window.gameStarted === false`）から可能
- **発射挙動**:
  - トリガーを引くと、現在選択されているボール（リンゴまたはキャベツ）が飛んでいく
  - 発射方向: コントローラーのraycaster方向
  - 発射速度: ゲーム中と同じ（既存の`speed`変数を使用）
  - 物理挙動: ゲーム中と同じ（重力、衝突判定など）
- **制約事項**:
  - スコアは加算されない
  - 的（動物）への当たり判定は行わない（または行っても無視）
  - ゲームタイマーは動かない
  - ボール数制限は適用（同時に3個まで）

### 3. ゲームスタート挙動の維持
- **変更なし**: 従来通りの動作を維持
- **開始方法**: コントローラーから出ている白い線（raycaster）を、Level1 EasyまたはLevel2 Hardのメニューに合わせてトリガーを引く
- **開始後の動作**: ゲーム開始後（`window.gameStarted === true`）は、従来通りにスコア加算、的の判定などが有効になる

## 非機能要件

### 1. パフォーマンス
- **既存動作への影響なし**: ゲーム中のパフォーマンスに影響を与えない
- **ボール同時発射制限**: 既存の3個制限を維持

### 2. ユーザビリティ
- **直感的な操作**: 既存のボタン配置・操作感を維持
- **視覚的フィードバック**: プレビュー表示の即時更新

### 3. 互換性
- **既存コードとの互換性**: 最小限の変更で実装
- **デバッグログ**: 既存のデバッグログシステムを活用

## 技術スタック
- **A-Frame**: 既存バージョン（1.x系）
- **カスタムコンポーネント**: 
  - `vr-controller`: ボタンハンドリング
  - `handle-shoot`: トリガー発射処理

## 成功基準

### 必須要件
- [ ] ゲーム開始前にグリップ/A/Bボタンでリンゴとキャベツが切り替わる
- [ ] ゲーム開始前にトリガーを引くとボールが発射される
- [ ] 発射されたボールは現在選択されているもの（リンゴまたはキャベツ）である
- [ ] プレビュー表示が即座に更新される
- [ ] ゲームスタートの挙動は変わらない（メニュー選択）
- [ ] ゲーム開始後も従来通りの動作をする

### 望ましい要件
- [ ] ゲーム前の発射ではスコアが加算されない
- [ ] ゲーム前の発射では的に当たっても反応しない
- [ ] ボール同時発射制限（3個）が適用される

## 現状分析

### 既存コード
- **ファイル**: `resources/views/shooting3Danimal.blade.php`
- **関連コンポーネント**:
  - `vr-controller` (行3244～3337): ボタンハンドリング
  - `handle-shoot` (行2150～2600付近): トリガー発射処理
- **問題点**:
  - `vr-controller`の`onButtonDown`メソッド（行3261-3268）: `if (!window.gameStarted || window.gameEnded) return;` でゲーム前は無視される
  - `handle-shoot`の`shoot`メソッド（行2298-2305）: `if (!window.gameStarted) return;` でゲーム前は発射できない

### 必要な変更
1. `vr-controller`の`onButtonDown`: ゲーム前でも動作するように条件を削除
2. `handle-shoot`の`shoot`: ゲーム前でも発射できるように条件を変更
3. スコア加算処理: `if (window.gameStarted)` で条件分岐を追加
4. 的の当たり判定: ゲーム前は無視するように条件追加

## リスクと制約

### リスク
1. **メニュー選択の誤作動**: ゲーム前のトリガー発射が、メニュー選択と競合する可能性
2. **パフォーマンス**: ゲーム前のボール発射が多すぎると、メモリリークや描画負荷の問題

### 制約
1. **既存動作の維持**: ゲーム中の動作は一切変更しない
2. **コード変更の最小化**: CLAUDE.mdの指示に従い、最小限の変更で実装

## 次のステップ
1. 設計フェーズへの移行（design.mdへの追記）
2. タスク化フェーズ（tasks.mdへの追記）
3. 実装フェーズ
---

# 要件定義: ARスタンプラリー202603版のlocalStorage分離

## 作成日時
2026年2月17日

## プロジェクト概要
ARstampRally.blade.phpをコピーして作成されたARstampRally202603.blade.phpにおいて、LocalStorageキーが重複しているため、/stamp と /stamp202603 の間でデータが共有されてしまう問題を解決する。

## 問題の現状
### 発生している問題
- ARstampRally202603.blade.php（/stamp202603）を開くと、元のARstampRally.blade.php（/stamp）で捕まえた動物がスタンプ帳に表示される
- 両ページで同じLocalStorageキーを使用しているため、データが混在している

### 共有されているLocalStorageキー
以下のキーが両ファイルで共通して使用されている：
1. `'ar-stamp-rally'` - スタンプデータの保存
2. `'ar-captured-animals'` - 捕獲済み動物の記録
3. `'ar-prize-exchanged'` - 景品交換済みフラグ
4. `'ar-prize-code'` - 景品交換コード
5. `'ar-user-id'` - ユーザーIDの保存（storageKey変数として使用）

## 目的
- /stamp と /stamp202603 のデータを完全に分離する
- それぞれ独立したスタンプラリーとして機能させる
- 既存の /stamp 側の動作には一切影響を与えない

## 成功基準
1. /stamp202603 で新しい動物を捕獲したとき、/stamp のスタンプ帳には反映されない
2. /stamp で捕獲した動物が /stamp202603 のスタンプ帳に表示されない
3. それぞれのページで独立したスタンプラリー進行状況が保持される
4. 既存の /stamp の動作が変更されていない（ARstampRally.blade.phpは変更しない）

## 技術的制約
- ARstampRally.blade.php（元のファイル）は一切変更しない
- ARstampRally202603.blade.phpのみを修正する
- 6908行の大規模ファイルのため、全てのlocalStorageキー使用箇所を漏れなく修正する必要がある
- 既存の機能やロジックを損なわない

## スコープ
### 対象範囲
- ARstampRally202603.blade.phpファイル内の全てのLocalStorageキーの変更
- 関連する全ての関数やイベントハンドラーの確認と修正

### 対象外
- ARstampRally.blade.phpの変更
- routes/web.phpの変更（既に適切にルーティング設定済み）
- データベースやAPIエンドポイントの変更

## 次のステップ
1. 設計フェーズ（design.mdへの追記）
2. タスク化フェーズ（tasks.mdへの追記）
3. 実装フェーズ

---

# 要件定義4: ARstampRally202603 スタンプ帳UI改善と管理画面統計追加

## 作成日時
2026年2月19日

## プロジェクト概要
ARstampRally202603.blade.phpの使用体験を向上させるため、スタンプ帳のUI改善を行い、また管理者が統計データを確認できるようにする。

## 背景
- 既存のARstampRally.blade.phpには管理ダッシュボード（admin/dashboard）があり、マーカースキャンの統計を確認できる
- ARstampRally202603.blade.phpも同様に統計データの確認が必要
- スタンプ帳のUIをより直感的にする必要がある

## 目的
1. スタンプ帳のUI改善により、未収集の動物への興味を喚起
2. 管理者がARstampRally202603.blade.phpの統計データを確認できるようにする

## 機能要件

### 1. スタンプ帳UI改善（ARstampRally202603.blade.php）

#### 1-1. ヒントボタンの非表示
**現状**: スタンプ帳モーダル内に「ヒントを見る」ボタンが存在（1931行目付近）
**変更後**: ボタンを完全に非表示にする

#### 1-2. 未収集動物名の「？？？」表示
**現状**: 
- シークレット動物（burger, kirin, namakemono, t-rex, panda）は未収集時に「シークレット」と表示
- 通常動物は未収集時でも実際の名前（「ひつじ」「きつね」など）が表示される

**変更後**:
- **パンダ（panda）**: 未収集時も「パンダ」と表示（変更なし）
- **シークレット動物（パンダ以外）**: 未収集時は「シークレット」と表示（変更なし）
- **通常動物（パンダとシークレット以外の全ての動物）**: 未収集時は「？？？」と表示

**対象動物**:
以下の動物が「？？？」表示の対象：
- sheep（ひつじ）
- fox（きつね）
- pengin（ペンギン）
- tonakai（トナカイ）
- pig（ぶた）
- tora（とら）
- gollira（ごりら）
- whiteDuck（白アヒル）
- araiguma（あらいぐま）
- wolf（おおかみ）
- duck（あひる）
- cat（ねこ）
- bear（くま）
- harinezumi（はりねずみ）
- hamstar（ハムスター）

### 2. 管理画面統計ページの追加

#### 2-1. 新規ページ作成
**目的**: ARstampRally202603.blade.phpのマーカースキャン統計を表示

**ページ情報**:
- URL: `/admin/dashboard202603`
- ビュー: `resources/views/admin/dashboard202603.blade.php`
- コントローラー: `AdminController` に新規メソッド `dashboard202603` を追加

#### 2-2. 統計データ
**表示内容**: とりあえずパンダ（marker_id: 'panda'）の統計のみ
- パンダマーカーの総スキャン数
- パンダマーカーのユニークユーザー数
- 最近のパンダスキャン履歴（日時、フィンガープリント、デバイス情報）

**データソース**: 
- `marker_scans` テーブル
- `marker_id = 'panda'` でフィルタリング

**フォーマット**:
- 既存の `admin/dashboard.blade.php` と同様のデザインとレイアウト
- パンダ専用にシンプル化

#### 2-3. ナビゲーション
**既存ダッシュボードからのリンク**:
- `admin/dashboard.blade.php` にARstampRally202603用のリンクを追加
- 相互にナビゲーションできるようにする

## 非機能要件

### パフォーマンス
- ページロード時間は既存のダッシュボードと同等
- スタンプ帳のレンダリング速度に影響を与えない

### セキュリティ
- 既存の管理画面認証を使用（session: admin_authenticated）
- 認証されていないユーザーはアクセス不可

### 保守性
- コードは既存のARstampRally202603.blade.phpの構造を維持
- 管理画面のコードは既存のdashboard.blade.phpと整合性を保つ

## 成功基準
1. ARstampRally202603.blade.phpのスタンプ帳で：
   - 「ヒントを見る」ボタンが表示されない
   - パンダは未収集時も「パンダ」と表示される
   - シークレット動物（パンダ以外）は未収集時に「シークレット」と表示される
   - 通常動物は未収集時に「？？？」と表示される
   - 収集済みの動物は実際の名前が表示される（変更なし）

2. 管理画面（/admin/dashboard202603）で：
   - パンダマーカーの統計データが正しく表示される
   - 既存の認証システムで保護されている
   - 既存のダッシュボードと統一感のあるデザイン

## 技術的制約
- ARstampRally202603.blade.phpは6951行の大規模ファイル
- showStampBook関数（3330行目付近）の修正が必要
- 既存の機能を損なわない
- 既存のARstampRally.blade.phpには影響を与えない

## スコープ

### 対象範囲
- ARstampRally202603.blade.phpのスタンプ帳UI部分
  - ヒントボタンの非表示
  - showStampBook関数の修正
- AdminControllerへの新規メソッド追加
- 新規ビューファイルの作成（admin/dashboard202603.blade.php）
- ルーティングの追加（routes/web.php）
- 既存ダッシュボードへのナビゲーションリンク追加

### 対象外
- ARstampRally.blade.phpの変更
- マーカースキャンAPIの変更
- データベーススキーマの変更
- 他の動物（パンダ以外）の統計表示（将来の拡張として残す）

## 依存関係
- 既存のMarkerScanモデル
- 既存のAdminController
- 既存の管理画面認証システム
- 既存のARstampRally202603.blade.phpのSTAMPSオブジェクト定義

## 次のステップ
1. 設計フェーズ（design.mdへの追記）- 詳細な実装設計
2. タスク化フェーズ（tasks.mdへの追記）- 具体的な作業手順
3. 実装フェーズ - コードの変更と追加

---

# 要件定義5: ARstampRally202603 - スタンプ帳アイコン表示改善 (追加修正)

## 作成日時
2026年2月19日

## 背景
前回の実装（要件定義4）で、スタンプ帳の未収集動物の名前表示を変更した（パンダ→「パンダ」、シークレット→「シークレット」、通常動物→「？？？」）。しかし、**アイコン表示は変更していなかった**ため、通常の15種の動物が未収集の場合、現在は絵文字（🐑🦊🐧など）がそのまま表示されている。

## 目的
未収集時のアイコン表示を統一し、ユーザーが「まだ見つけていない動物」であることをより明確に認識できるようにする。

## 要件

### 現在の仕様（前回実装後）
**未収集時のアイコン表示**:
- **パンダ**: 🐾（足跡）
- **シークレット動物（パンダ以外）**: 🐾（足跡）
- **通常動物15種**: 🐑🦊🐧などの絵文字（**そのまま表示** ← 問題点）

**未収集時の名前表示**:
- **パンダ**: 「パンダ」
- **シークレット動物（パンダ以外）**: 「シークレット」
- **通常動物15種**: 「？？？」

### 新しい仕様
**未収集時のアイコン表示**:
- **パンダ**: 🐾（足跡） ← 変更なし
- **シークレット動物（パンダ以外）**: 🐾（足跡） ← 変更なし
- **通常動物15種**: 🐾（足跡） ← **絵文字から足跡に変更**

**収集済みのアイコン表示**: 変更なし
- スクリーンショットがあれば表示
- なければ絵文字（🐑🦊🐧など）を表示

**名前表示**: 前回の実装のまま（変更なし）

## 影響範囲
- **対象ファイル**: `resources/views/ARstampRally202603.blade.php`
- **対象関数**: `showStampBook()` 関数（3340行目～3420行目付近）
- **対象箇所**: 通常動物の未収集時のアイコン表示ロジック（約3390行目）

## 技術的詳細

### 現在のコード（3390行目付近）
```javascript
} else {
    // 通常動物の未収集時: '？？？' を表示
    // iconContentはデフォルトのまま（stamp.icon）
    nameText = '？？？';
}
```

### 変更後のコード
```javascript
} else {
    // 通常動物の未収集時: '？？？' を表示、アイコンは足跡
    iconContent = '🐾'; // 足跡アイコンに変更
    nameText = '？？？';
}
```

## 非機能要件
- 既存の収集済みアイコン表示には影響を与えない
- シークレット動物の表示ロジックには影響を与えない
- パフォーマンスに影響を与えない（単純なアイコン変更のみ）

## 成功基準
1. 未収集の通常動物15種のアイコンが🐾（足跡）で表示される
2. 未収集のパンダとシークレット動物は引き続き🐾（足跡）で表示される
3. 収集済みの動物はスクリーンショットまたは絵文字が表示される（変更なし）
4. 動物名の表示は前回の実装のまま（パンダ/シークレット/？？？）

## スコープ

### 対象範囲
- showStampBook関数内の通常動物未収集時のアイコン表示ロジック（1行の追加）

### 対象外
- 動物名の表示ロジック（前回実装済み）
- 収集済み動物の表示ロジック
- シークレット動物の表示ロジック
- その他のARstampRally202603.blade.phpの機能

## 次のステップ
1. 設計フェーズ（design.mdへの追記）- 詳細な実装設計
2. タスク化フェーズ（tasks.mdへの追記）- 具体的な作業手順
3. 実装フェーズ - コードの変更（1行の追加）

---

# 要件定義6: ARstampRally202603 - マーカー検出とボールヒットの統計分離

## 作成日時
2026年2月19日

## 背景
現在、admin/dashboard202603ではパンダマーカーの統計を表示しているが、以下の問題がある：
1. **マーカー検出（ARマーカーを読み取った）とボールヒット（ボールをぶつけてゲット）が区別されていない**
2. **マーカー検出時にデータが記録されていない** - ボールヒット時（新規ゲット時）のみ記録
3. **重複カウントの問題** - 同じ日に同じマーカーを複数回検出した場合、カウントアップされてしまう

### 現在の実装状況
**データフロー**:
```
1. markerFoundイベント発火（731行目）
   ↓
   isAnimalCaptured()でLocalStorageチェック
   ↓
   未捕獲 → モデル表示
   捕獲済み → メッセージ表示
   ↓
   【記録なし】← 問題点

2. ボールヒット（handleHit関数、340行目）
   ↓
   playHitAnimation実行
   ↓
   collectStamp呼び出し（124行目）
   ↓
   新規ゲット時のみrecordMarkerScan呼び出し（143行目）
   ↓
   marker_scansテーブルに記録（capture_typeなし）
```

**データベース構造**（現在）:
```sql
marker_scans:
  - marker_id, marker_name, fingerprint
  - scanned_at, scan_count
  - device_info, user_agent, ip_address
  - ※capture_type（マーカー検出 or ボールヒット）の区別なし
```

## 目的
マーカー検出回数とボールヒット回数を分けて統計を取り、ダッシュボードで各動物ごとに2つの数値を表示できるようにする。

## ユースケース

### UC1: マーカー検出の記録
**アクター**: ユーザー（ARアプリ利用者）  
**前提条件**: ARマーカーをカメラで読み取る  
**トリガー**: markerFoundイベント発火  
**メインフロー**:
1. マーカーが検出される
2. isAnimalCaptured()で捕獲済みかチェック
3. **未捕獲の場合のみ記録処理を実行**（新要件）
4. 同じ端末・同じマーカー・同じ日付の記録があるかチェック（新要件）
5. なければmarker_scansテーブルに記録（capture_type: 'marker_scan'）（新要件）
6. あれば記録しない（重複防止）（新要件）

**成功条件**:
- 未捕獲のマーカーを初めて検出した場合、marker_scansに記録される
- 同じ日に同じマーカーを再検出しても、カウントアップされない
- 捕獲済みのマーカーは記録されない

### UC2: ボールヒットの記録
**アクター**: ユーザー（ARアプリ利用者）  
**前提条件**: ボールを投げて動物にヒット  
**トリガー**: handleHit関数実行  
**メインフロー**:
1. ボールが動物にヒット
2. playHitAnimation実行
3. collectStamp呼び出し
4. 新規ゲットの場合、recordMarkerScan呼び出し
5. **capture_type: 'ball_hit'を指定して記録**（変更点）

**成功条件**:
- 新規ゲット時にmarker_scansに記録される（capture_type: 'ball_hit'）
- 既にゲット済みの動物は記録されない（既存の動作）

### UC3: ダッシュボードでの統計表示
**アクター**: 管理者  
**前提条件**: admin/dashboard202603にログイン済み  
**トリガー**: ダッシュボードページアクセス  
**メインフロー**:
1. admin/dashboard202603にアクセス
2. 各動物ごとに以下を表示：
   - マーカー検出回数（capture_type: 'marker_scan'のカウント）
   - ボールヒット回数（capture_type: 'ball_hit'のカウント）
   - ユニークユーザー数
   - 最近のスキャン履歴（capture_typeも表示）
3. 日別の統計グラフ（2つのタイプ別に集計）

**成功条件**:
- 各動物のマーカー検出回数とボールヒット回数が正しく表示される
- ページネーションが機能する
- グラフが2つのタイプ別に表示される

## 機能要件

### FR1: マーカー検出の記録機能
**優先度**: 高  
**詳細**:
- markerFoundイベント時に記録処理を追加
- 未捕獲の動物のみ記録（isAnimalCaptured()でチェック）
- 同じ端末・同じマーカー・同じ日付の記録があるかチェック
- なければmarker_scansテーブルに記録（capture_type: 'marker_scan'）
- 日付の粒度: 日単位（2026-02-19）

**実装箇所**:
- ARstampRally202603.blade.php の markerFoundイベントリスナー（731行目付近）
- 新しいAPI関数 recordMarkerDetection() を作成

### FR2: データベーススキーマの拡張
**優先度**: 高  
**詳細**:
- marker_scansテーブルにcapture_typeカラムを追加
- データ型: ENUM('marker_scan', 'ball_hit') または VARCHAR(20)
- デフォルト値: 'ball_hit'（既存データとの互換性）
- インデックス追加: capture_type

**マイグレーション**:
- 新しいマイグレーションファイルを作成
- 既存のmarker_scansレコードのcapture_typeを'ball_hit'に設定

### FR3: 既存のボールヒット記録の修正
**優先度**: 高  
**詳細**:
- recordMarkerScan関数にcapture_typeパラメータを追加
- collectStamp関数からrecordMarkerScan呼び出し時、capture_type: 'ball_hit'を指定

**実装箇所**:
- ARstampRally202603.blade.php の recordMarkerScan関数（2789行目）
- collectStamp関数（143行目）

### FR4: 重複チェック機能
**優先度**: 高  
**詳細**:
- 同じ端末（fingerprint）・同じマーカー（marker_id）・同じ日付（scanned_atの日付部分）の記録があるかチェック
- LocalStorageにキャッシュして、API呼び出しを削減
- キャッシュキー: `marker-scan-cache-202603-${markerId}-${date}`

**実装箇所**:
- ARstampRally202603.blade.php に新関数 hasMarkerScannedToday()
- MarkerScanController に新メソッド checkDuplicate()

### FR5: ダッシュボードの拡張
**優先度**: 高  
**詳細**:
- 全動物（20種）の統計を表示（パンダだけでなく）
- 各動物ごとに2つの数値を表示：
  - マーカー検出回数
  - ボールヒット回数
- テーブル形式で表示（ソート可能）
- グラフは2つのタイプ別に表示（積み上げ棒グラフ or 2本の棒グラフ）

**実装箇所**:
- AdminController の dashboard202603メソッド
- dashboard202603.blade.php のビューテンプレート

## 非機能要件

### NFR1: パフォーマンス
- マーカー検出時のAPI呼び出しが既存のボールヒット時の記録と同等のパフォーマンス
- LocalStorageキャッシュで重複チェックのAPI呼び出しを削減
- ダッシュボードのページロード時間が2秒以内

### NFR2: データ整合性
- 既存のmarker_scansデータを損なわない
- マイグレーション時に既存データのcapture_typeを'ball_hit'に設定
- ロールバックが可能

### NFR3: 保守性
- recordMarkerScan関数の既存の呼び出し箇所を壊さない
- 新しいパラメータはオプショナル（デフォルト値: 'ball_hit'）

### NFR4: セキュリティ
- CSRF保護（既存のrecordMarkerScan関数と同様）
- フィンガープリント検証
- SQLインジェクション対策（Eloquent使用）

## 技術的制約

### TC1: 大規模ファイルの編集
- ARstampRally202603.blade.phpは6964行の大規模ファイル
- 複数箇所の修正が必要（markerFoundイベント、recordMarkerScan関数等）
- 慎重な編集とテストが必要

### TC2: 既存データとの互換性
- 既存のmarker_scansレコード（約数百～数千件）が存在する可能性
- マイグレーション時に既存データのcapture_typeを設定
- 統計表示時に既存データも正しく集計

### TC3: ブラウザ互換性
- LocalStorageのキャッシュ機能が動作するブラウザ
- 既存のフィンガープリント生成機能との整合性

## データモデル

### marker_scans テーブル（拡張後）
```sql
CREATE TABLE marker_scans (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    session_id VARCHAR(255) NOT NULL,
    fingerprint VARCHAR(255) NULL,
    marker_id VARCHAR(255) NOT NULL,
    marker_name VARCHAR(255) NOT NULL,
    scan_count INT DEFAULT 1,
    capture_type VARCHAR(20) DEFAULT 'ball_hit', -- 新規追加
    scanned_at TIMESTAMP NOT NULL,
    user_agent VARCHAR(255),
    ip_address VARCHAR(45),
    device_info JSON,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    KEY idx_session_id (session_id),
    KEY idx_fingerprint (fingerprint),
    KEY idx_marker_id (marker_id),
    KEY idx_capture_type (capture_type), -- 新規追加
    KEY idx_session_marker (session_id, marker_id),
    KEY idx_fingerprint_marker (fingerprint, marker_id),
    KEY idx_scanned_at (scanned_at)
);
```

### LocalStorageキャッシュ
```javascript
// キー: marker-scan-cache-202603-{markerId}-{date}
// 値: { scanned: true, timestamp: '2026-02-19T10:30:00Z' }
// 有効期限: 当日のみ（日付が変わったらクリア）
```

## 成功基準

### 必須条件
- [ ] マーカー検出時にmarker_scansに記録される（capture_type: 'marker_scan'）
- [ ] ボールヒット時にmarker_scansに記録される（capture_type: 'ball_hit'）
- [ ] 同じ日に同じマーカーを再検出しても、カウントアップされない
- [ ] 捕獲済みのマーカーは記録されない
- [ ] ダッシュボードで各動物のマーカー検出回数とボールヒット回数が表示される
- [ ] 既存のmarker_scansデータが正しく集計される
- [ ] 既存機能（スタンプ収集、スタンプ帳表示等）が損なわれない

### 望ましい条件
- [ ] ダッシュボードでソート機能が動作する
- [ ] グラフが2つのタイプ別に表示される
- [ ] LocalStorageキャッシュでAPI呼び出しが削減される

## スコープ

### 対象範囲
- marker_scansテーブルのスキーマ拡張（capture_typeカラム追加）
- ARstampRally202603.blade.php の markerFoundイベントリスナーの修正
- recordMarkerScan関数の拡張（capture_typeパラメータ追加）
- MarkerScanController の record メソッドの修正
- AdminController の dashboard202603 メソッドの拡張
- dashboard202603.blade.php の表示内容拡張

### 対象外
- ARstampRally.blade.php の変更（元のファイルは変更しない）
- 他のダッシュボードページの変更
- マーカースキャン以外の統計機能
- リアルタイム更新機能

## 依存関係
- 既存のMarkerScanモデル
- 既存のMarkerScanController
- 既存のrecordMarkerScan関数
- 既存のgenerateFingerprint関数
- 既存のisAnimalCaptured関数

## リスク分析

### リスク1: 大規模ファイル編集のミス
**影響度**: 高  
**発生確率**: 中  
**対策**: 
- 変更前に該当行の周辺コードを確認（10行前後）
- バックアップ（Git commit）
- 段階的な実装とテスト

### リスク2: 既存データとの互換性問題
**影響度**: 中  
**発生確率**: 低  
**対策**:
- マイグレーション時に既存データのcapture_typeを'ball_hit'に設定
- ロールバック用のマイグレーションを用意

### リスク3: パフォーマンス劣化
**影響度**: 中  
**発生確率**: 低  
**対策**:
- LocalStorageキャッシュで重複チェックのAPI呼び出しを削減
- インデックス追加（capture_type）

### リスク4: マーカー検出の過剰記録
**影響度**: 低  
**発生確率**: 低  
**対策**:
- LocalStorageキャッシュで同じ日の重複を防止
- 捕獲済み動物は記録しない

## 次のステップ
1. 設計フェーズ（design.mdへの追記）- 詳細な実装設計
2. タスク化フェーズ（tasks.mdへの追記）- 具体的な作業手順
3. 実装フェーズ - コードの変更と追加